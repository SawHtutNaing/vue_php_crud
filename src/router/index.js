import { createRouter, createWebHistory } from 'vue-router';

import Myown from '@/My.vue';

const routes = {
//  'new':Myown 

};

const router = createRouter({
  history: createWebHistory(),
  routes,
});

export default router;
